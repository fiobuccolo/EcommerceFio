import React from 'react';
import React from 'react'; // para guardar componentes que no se modifican

export const Footer = React.memo(() => {
    return (
        <div>
            
        </div>
    );
}

)
